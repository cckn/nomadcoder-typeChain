/**
 * Initialize the myPackage module.
 * @param {object} config
 * @param {string} config.url
 * @param {boolean} config.isDebug
 * @returns {boolean} true if successful
 */
export function init(config) {
	return true
}

/**
 * Exit the myPackage module.
 * @param {number} code
 * @returns {boolean} true if successful
 */
export function exit(code) {
	return code + 1
}
